// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("yac", "for yac support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("yac", "enable yac support", "no");

if (PHP_YAC != "no") {
	EXTENSION("yac", "yac.c");
}

